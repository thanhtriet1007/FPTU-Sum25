#include <stdio.h>
#include <stdlib.h>

int main() {
	int x, y, z, t;
	
	printf("Hay nhap lan luot gia tri cho x, y, z, t:");
	
	scanf("%d%d%d%d", &x, &y, &z, &t);
	
	printf("%d %d %d %d", x, y, z, t);
	return 0;
}